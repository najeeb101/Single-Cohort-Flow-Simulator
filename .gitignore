# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# ---- Virtual environments ----
.venv/
venv/
env/
ENV/

# ---- Testing / coverage ----
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# ---- Claude Code ----
# Keep the shared project settings; ignore personal/local state.
.claude/*
!.claude/settings.json
.claude/settings.local.json

# ---- IDE / editor ----
.vscode/
.idea/
*.swp
*~

# ---- OS noise ----
.DS_Store
Thumbs.db
desktop.ini

# ---- Project-specific ----
# outputs/figures/ stays COMMITTED: report/report.md embeds those PNGs so it renders on GitHub.
# outputs/reports/ is regenerated raw data (CSV + the 16k-line flow_timeline.json) embedded
# nowhere — keep it out of history. web/ gets the same shape live from POST /simulate, not this file.
outputs/reports/
# data/qu_raw/    # downloaded QU open-data CSVs (validation only; re-downloadable via scripts/analyze_qu_data.py)
